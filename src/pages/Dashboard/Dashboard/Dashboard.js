import React from 'react';
import Loading from '../../Shared/Loading/Loading';

const Dashboard = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default Dashboard;